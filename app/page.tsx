// Placeholder page for all interactions via api routes
// In a full-stack version, this would be the chat UI
export default function Home() {
  return <p>Subscription Agent API - use Post /api/agent/chat</p>;
}
